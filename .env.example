# Supabase — client-side (exposed to browser, safe to publish)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — server-side ONLY (bypasses Row Level Security — never expose in browser)
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI (Whisper API — transcrição de voz)
OPENAI_API_KEY=